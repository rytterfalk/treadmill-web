[Service]
# Keep persistent data OUTSIDE the git worktree to avoid accidental wipes during deploy.
#
# Option A (recommended): point to the repo's 7min/.env so DB_PATH/UPLOAD_DIR live in one place
# EnvironmentFile=/home/carlrytterfalk/treadmill-web/7min/.env
#
# Option B: define paths here (uncomment and adjust)
# Environment=DB_PATH=/var/lib/7min/app.db
# Environment=UPLOAD_DIR=/var/lib/7min/uploads

